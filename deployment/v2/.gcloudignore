# This directory also holds large scratch/training data (multi-GB .wav
# files, dataset zips, notebooks) that must never enter the build context —
# ignore everything by default, then explicitly allow only what the
# Dockerfile actually COPYs.
*

!Dockerfile
!app_v2.py
!realtime_sessions.py
!real_time_audio_v2.py
!history_store.py
!requirements-prod.txt

!drone_detection
!drone_detection/**

!models
!models/**

!templates
!templates/**
